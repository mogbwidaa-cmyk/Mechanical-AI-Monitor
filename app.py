import streamlit as st
import pandas as pd
import numpy as np
import plotly.graph_objects as go
import cv2
from PIL import Image
import datetime
from fpdf import FPDF
import base64

# --- إعدادات الصفحة ---
st.set_page_config(page_title="نظام التشخيص الميكانيكي الذكي", layout="wide")

# --- دالة توليد تقرير PDF المحدثة ببياناتك الشخصية ---
def create_pdf(vibration, status, temp, rul):
    pdf = FPDF()
    pdf.add_page()
    pdf.set_font("Arial", 'B', 16)
    
    # عنوان التقرير
    pdf.cell(200, 10, txt="Mechanical Inspection Report", ln=True, align='C')
    pdf.ln(10)
    
    # بيانات المهندس مجاهد
    pdf.set_font("Arial", size=12)
    pdf.cell(200, 10, txt=f"Date: {datetime.date.today()}", ln=True)
    pdf.cell(200, 10, txt=f"Lead Engineer: Mogahed Bashir Ahmed", ln=True)
    pdf.cell(200, 10, txt=f"Location: Madinah Al Munawwarah, KSA", ln=True)
    pdf.cell(200, 10, txt=f"Phone: +966501318054", ln=True)
    
    # حالة التوظيف باللون الأحمر للفت الانتباه
    pdf.set_text_color(200, 0, 0)
    pdf.cell(200, 10, txt="Status: Available for Hire / Ready to Start Immediately", ln=True)
    pdf.set_text_color(0, 0, 0)
    
    pdf.ln(5)
    pdf.cell(200, 10, txt="--------------------------------------------------", ln=True)
    
    # النتائج التقنية
    pdf.set_font("Arial", 'B', 14)
    pdf.cell(200, 10, txt="Technical Analysis Results:", ln=True)
    pdf.set_font("Arial", size=12)
    pdf.cell(200, 10, txt=f"- Vibration Level: {vibration} mm/s", ln=True)
    pdf.cell(200, 10, txt=f"- Machine Status (ISO 10816): {status}", ln=True)
    pdf.cell(200, 10, txt=f"- Operating Temperature: {temp} C", ln=True)
    pdf.cell(200, 10, txt=f"- Predicted Remaining Useful Life (RUL): {int(rul)} Days", ln=True)
    
    pdf.ln(20)
    pdf.set_font("Arial", 'I', 10)
    pdf.cell(200, 10, txt="Generated by AI Mechanical Monitor Platform", ln=True, align='C')
    
    return pdf.output(dest='S').encode('latin-1')

# --- واجهة المستخدم الرئيسية ---
st.title("🛠️ المنصة الذكية لمراقبة المعدات الميكانيكية")

# القائمة الجانبية (Sidebar) مع الحالة الوظيفية
st.sidebar.header("👤 بيانات المهندس المسؤول")
st.sidebar.write("**المهندس:** مجاهد بشير أحمد")
st.sidebar.write("📍 **الموقع:** المدينة المنورة، السعودية")
st.sidebar.write("📞 **الجوال:** `+966501318054` ")
st.sidebar.success("✅ **متاح للتوظيف فوراً**")

# رابط لينكد إن (تأكد من تحديث الرابط الفعلي في المتصفح)
st.sidebar.markdown("[🔗 LinkedIn Profile](https://www.linkedin.com/in/mogahed-bashir)")

st.sidebar.divider()
vibration = st.sidebar.slider("مستوى الاهتزاز (mm/s)", 0.0, 15.0, 4.5)
temp = st.sidebar.number_input("درجة الحرارة (°C)", value=65)

# --- منطق التحليل التشخيصي ---
if vibration <= 2.8:
    status = "Good (Zone A)"
    color = "green"
elif vibration <= 7.1:
    status = "Satisfactory (Zone B)"
    color = "orange"
else:
    status = "Unacceptable (Zone D)"
    color = "red"

col1, col2 = st.columns([1, 1])
with col1:
    fig = go.Figure(go.Indicator(
        mode = "gauge+number",
        value = vibration,
        title = {'text': "Vibration Severity"},
        gauge = {'axis': {'range': [0, 15]},
                 'bar': {'color': color},
                 'steps': [
                     {'range': [0, 2.8], 'color': "#a3cfbb"},
                     {'range': [2.8, 7.1], 'color': "#fff3cd"},
                     {'range': [7.1, 15], 'color': "#f8d7da"}]}))
    st.plotly_chart(fig, use_container_width=True)

with col2:
    rul_prediction = max(0, 100 - (vibration * 5) - (temp * 0.2))
    st.metric("العمر الافتراضي المتبقي (RUL)", f"{int(rul_prediction)} يوم")
    st.progress(int(rul_prediction) / 100)
    st.write(f"الحالة التشخيصية: **{status}**")

# --- الفحص البصري ---
st.divider()
st.header("📸 الفحص البصري (Computer Vision)")
uploaded_file = st.file_uploader("ارفع صورة للفحص السطحي...", type=['jpg', 'png', 'jpeg'])

if uploaded_file:
    image = Image.open(uploaded_file)
    img_array = np.array(image)
    gray = cv2.cvtColor(img_array, cv2.COLOR_RGB2GRAY)
    edges = cv2.Canny(gray, 50, 150)
    c1, c2 = st.columns(2)
    with c1: st.image(image, caption="الصورة الأصلية", use_container_width=True)
    with c2: st.image(edges, caption="تحليل الشروخ والتآكل", use_container_width=True)

# --- زر التقرير ---
st.divider()
if st.button("توليد تقرير PDF مع بيانات التواصل"):
    pdf_content = create_pdf(vibration, status, temp, rul_prediction)
    b64 = base64.b64encode(pdf_content).decode('utf-8')
    href = f'<a href="data:application/octet-stream;base64,{b64}" download="Mogahed_Maintenance_Report.pdf">📥 تحميل التقرير (بيانات المهندس مجاهد)</a>'
    st.markdown(href, unsafe_allow_html=True)
if st.button("توليد تقرير PDF مع بيانات التواصل"):
    pdf_content = create_pdf(vibration, status, temp, rul_prediction)
    b64 = base64.b64encode(pdf_content).decode('utf-8')
    href = f'<a href="data:application/octet-stream;base64,{b64}" download="Mogahed_Maintenance_Report.pdf">📥 اضغط هنا لتحميل التقرير</a>'
    st.markdown(href, unsafe_allow_html=True)
    st.success("تم تجهيز التقرير بنجاح!")
